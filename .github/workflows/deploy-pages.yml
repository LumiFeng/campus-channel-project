name: Verify GitHub Actions

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Verify runner
        run: echo "GitHub Actions is available"
